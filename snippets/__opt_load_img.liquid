{% capture LOAD_IMG %}

	{%- comment -%}
	Lazyload
	https://github.com/aFarkas/lazysizes
	https://github.com/aFarkas/lazysizes/tree/gh-pages/plugins/rias
	{%- endcomment -%}

	{%- assign exists = true -%}

	{%- if src == null -%}
		{%- assign exists = false -%}		
	{%- endif -%}

	{%- if src contains 'no-image' -%}
		{%- assign exists = false -%}
	{%- endif -%}

	{%- if exists == true -%}
		{% capture data-srcset %}
			{% assign replace = '_{width}x' %}
			{{ src | replace: '_10x', replace }}
		{% endcapture %}

		data-widths="[180, 360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]" data-sizes="auto" src="data:image/gif;base64,R0lGODlhAQABAIAAAP///wAAACH5BAEAAAAALAAAAAABAAEAAAICRAEAOw==" data-src="{{ data-srcset }}" class="lazyload {{ class }}"

	{%- else -%}
		
		class="lazyload {{ class }}"

	{%- endif -%}

{% endcapture %}{{ LOAD_IMG | strip | replace:'	' | strip_newlines }}